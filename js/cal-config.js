/**
 * Cal.com booking configuration.
 *
 * 1. Create a free account at cal.com and set your username below.
 * 2. Create an Event Type for each key below (any slug you like) and
 *    paste its slug in as the value.
 * 3. Leave CAL_USERNAME as null until step 1 is done — booking
 *    buttons fall back to the contact form until then.
 */
var CAL_USERNAME = null; // e.g. "wellbeyondnow"

var CAL_EVENTS = {
  discoveryCall: null,   // Free 20-min Discovery Call
  singleSession: null,   // 90-min 1:1 Coaching Session — $150
  postpartumVisit: null  // In-home Postpartum Visit
};
